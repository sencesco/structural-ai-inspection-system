# CLAUDE.md

This repository uses `AGENTS.md` as the canonical agent context.

Before modifying code, read:
1. `AGENTS.md`
2. The relevant file under `docs/agent/`
3. Only the target source files needed for the task

Use `docs/agent/context.md` only for architecture-level changes.
Use `skills/` only for repeated task workflows.
Do not scan the whole repository unless the task requires architecture-level changes.
Use this file only as a thin pointer. Do not duplicate project brain here.

For frontend design work, use the `frontend-design` skill. If needed, read `SKILL.md`.