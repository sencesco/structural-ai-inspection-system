services:
  api:
    build:
      context: ./apps/api
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  web:
    build:
      context: ./apps/web
    ports:
      - "3000:3000"
    command: npm run dev

