# Release Process

Placeholder for versioned demo releases.

- Run API, web, ML, RAG, and system tests.
- Update `VERSION` and `CHANGELOG.md`.
- Confirm model and dataset registry entries.
- Build and smoke-test Docker services.
