name: ML Eval

on:
  pull_request:
  workflow_dispatch:

jobs:
  ml-eval:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Run ML evaluation here"
